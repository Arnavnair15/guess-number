# guess-number

import random
num = random.randint(0, 10)
guess = None
while guess != num:
    guess = input("guess a number between 1 to 10:")
    guess = int(guess)
    
    if guess == num:
        print("congratulations! you won!")
        break
    else:
        print("you predict the wrong number, please try again!")
